alias e='exit'
alias l='ls -A'
alias la='ls -Al'
alias ll='ls -l'
alias n='nvim'
alias python='python3'
alias cls='clear'
alias vi='vim'
alias javac="javac -J-Dfile.encoding=utf8"
alias grep="grep --color=auto"

# file
alias hosts='sudo nvim /etc/hosts'
alias auto_commit='bash ~/.config/bin/auto-commit.sh'
alias daydayone='bash ~/github.com/study_git/commit.sh'

# gist
alias idea='nvim ~/github.com/002_gist/idea/idea.md'
alias todo='nvim ~/github.com/002_gist/todo/todo.md'

# git
alias gst="git status"

alias -s html=open   # 在命令行直接输入后缀为 html 的文件名，会在 TextMate 中打开
alias -s rb=nvim     # 在命令行直接输入 ruby 文件，会在 TextMate 中打开
alias -s py=nvim     # 在命令行直接输入 python 文件，会用 vim 中打开，以下类似
alias -s js=nvim
alias -s c=nvim
alias -s java=nvim
alias -s txt=nvim
alias -s gz='tar -xzvf'
alias -s tgz='tar -xzvf'
alias -s zip='unzip'
alias -s bz2='tar -xjvf'

